# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
sudo: false
cache: packages

install:
  - ./travis-tool.sh install_bioc Rgraphviz
  - ./travis-tool.sh install_bioc graph